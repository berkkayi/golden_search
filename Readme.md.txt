Golden search algoritması main.m dosyası ile çalışmaktadır.
main dosyası, golden_search algoritmasını ve optimize edeceği func 
fonksiyonunu çağırıyor. Çağırdığı fonksiyonlara ek olarak minimum değer,
maksimum değer ve epsilon değerini girdi olarak alıyor.
almış olduğu girdiler ile optimum iterasyon sayısını bularak
optimize edilecek fonksiyonun optimum girdi değerini, fonksiyon çıktısını ve
iterasyon sayısını çıktı olarak veriyor. main dosyasının çalıştırılması yeterlidir. 

main.m
	golden_search.m (main function)
	func.m (model input function)